# Copy to ".env" and fill in after the Sanity project is created.
# These are public (safe to expose): they identify a read-only dataset.
PUBLIC_SANITY_PROJECT_ID=placeholder
PUBLIC_SANITY_DATASET=production
