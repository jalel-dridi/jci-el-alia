# Copy this file to ".env" and fill in the values after running `sanity login`
# and creating the project. These are safe to expose (public project id/dataset).
SANITY_STUDIO_PROJECT_ID=REPLACE_WITH_PROJECT_ID
SANITY_STUDIO_DATASET=production
